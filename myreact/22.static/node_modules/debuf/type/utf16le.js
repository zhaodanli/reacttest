

function encode(buf, val, offset, size) {
  return buf.write(val, offset, size, 'utf16le');
}

function decode(buf, offset, size) {
  return buf.toString('utf16le', offset, offset + size);
};

function size(val) {
  return Buffer.byteLength(val, 'utf16le');
}


exports.type   = 'string';
exports.size   = size;
exports.decode = decode;
exports.encode = encode;
