

function encode(buf, val, offset, size) {
  buf.write(val, offset, size, 'base64');
}

function decode(buf, offset, size) {
  return buf.toString('base64', offset, offset + size);
};

function size(val) {
  return Buffer.byteLength(val, 'base64');
}


exports.type   = 'string';
exports.size   = size;
exports.decode = decode;
exports.encode = encode;
