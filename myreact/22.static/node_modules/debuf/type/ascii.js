

function encode(buf, val, offset, size) {
  buf.write(val, offset, size, 'ascii');
}

function decode(buf, offset, size) {
  return buf.toString('ascii', offset, offset + size);
};

function size(val) {
  return Buffer.byteLength(val, 'ascii');
}


exports.type   = 'string';
exports.size   = size;
exports.decode = decode;
exports.encode = encode;
