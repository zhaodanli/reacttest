

function encode(buf, val, offset, size) {
  return buf.write(val, offset, size, 'hex');
}

function decode(buf, offset, size) {
  return buf.toString('hex', offset, offset + size);
};

function size(val) {
  return Buffer.byteLength(val, 'hex');
}


exports.type   = 'string';
exports.size   = size;
exports.decode = decode;
exports.encode = encode;
